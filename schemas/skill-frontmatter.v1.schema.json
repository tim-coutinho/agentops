{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "Skill Frontmatter",
  "description": "Schema for AgentOps SKILL.md YAML frontmatter",
  "type": "object",
  "required": ["name", "description", "skill_api_version"],
  "properties": {
    "name": { "type": "string" },
    "description": { "type": "string" },
    "skill_api_version": { "type": "integer", "const": 1 },
    "metadata": {
      "type": "object",
      "properties": {
        "tier": { "type": "string", "enum": ["judgment", "execution", "library", "session", "product", "contribute", "meta", "background", "orchestration"] },
        "dependencies": { "type": "array" },
        "internal": { "type": "boolean" },
        "version": { "type": "string" },
        "author": { "type": "string" },
        "triggers": { "type": "array", "items": { "type": "string" } },
        "replaces": { "type": "string" }
      }
    },
    "user-invocable": { "type": "boolean" },
    "context": { "type": "string" },
    "allowed-tools": { "type": "array", "items": { "type": "string" } },
    "license": { "type": "string" },
    "compatibility": { "type": "string" },
    "model": { "type": "string", "description": "Preferred model for skill execution (e.g., haiku, sonnet)" }
  },
  "additionalProperties": false
}
